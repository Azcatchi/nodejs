extends ../layout

block content
	h1 Create new team
	- warning = warning
	if warning
		p Veuillez remplir tous les champs
	- taken = taken
	if taken
		p nom de team déjà pris
	form(method="post", action= action)
		div
			label(for="teamName") Chose one team name :
			div
				input(type="text", name="teamName")
		div.form-group
			div
				p
				input(type="submit", value="Enregistrer")
